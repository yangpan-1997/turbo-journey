# 🍅 番茄钟 - 网络使用指南

## 快速开始（3 步启动）

### Windows 用户

1. **双击启动脚本**
   ```
   start_server.bat
   ```
   
2. **在浏览器中打开**
   ```
   http://localhost:8000/pomodoro.html
   ```

3. **在手机打开**
   - 打开 http://192.168.x.x:8000/info （用你的电脑IP替换）
   - 或扫描页面上的二维码

### Mac/Linux 用户

1. **运行启动脚本**
   ```bash
   chmod +x start_server.sh
   ./start_server.sh
   ```

2. **在浏览器中打开**
   ```
   http://localhost:8000/pomodoro.html
   ```

3. **在手机打开**
   - 打开 http://[你的IP]:8000/info
   - 或扫描页面上的二维码

---

## 访问方式

### 方式 1️⃣：本地访问（仅限当前电脑）
```
http://localhost:8000/pomodoro.html
```

### 方式 2️⃣：局域网访问（手机、平板、其他电脑）
```
http://192.168.1.100:8000/pomodoro.html
```
替换 `192.168.1.100` 为你的电脑 IP 地址

### 方式 3️⃣：通过信息页面
```
http://[你的IP]:8000/info
```
页面会自动显示二维码，扫一下就能在手机上使用

### 方式 4️⃣：直接访问信息页面
服务器启动后会显示完整的 IP 地址和端口
复制 URL 到手机浏览器即可

---

## 查找你的电脑 IP 地址

### Windows
1. 按 `Win + R`
2. 输入 `cmd` 按回车
3. 输入 `ipconfig` 按回车
4. 查找 `IPv4 地址` （通常是 192.168.x.x）

### Mac
1. 打开终端
2. 输入：`ifconfig | grep inet`
3. 查找不是 127.0.0.1 的地址

### Linux
1. 打开终端
2. 输入：`hostname -I`
3. 第一个地址就是你的 IP

---

## 🔧 进阶配置

### 修改端口号
默认使用 8000 端口，如要修改：

```bash
python3 run_server.py 8080          # 使用 8080 端口
python3 run_server.py 9000 0.0.0.0  # 使用 9000 端口，所有网络接口
```

### 仅本地访问（安全考虑）
```bash
python3 run_server.py 8000 localhost  # 仅本地可访问
```

### 允许远程访问（需要开启防火墙）
```bash
python3 run_server.py 8000 0.0.0.0  # 所有网络接口
```

---

## 📱 手机使用说明

### iOS（iPhone/iPad）
1. 打开 Safari 浏览器
2. 在地址栏输入：`http://[电脑IP]:8000/pomodoro.html`
3. 点击"分享" → "添加到主屏幕"（创建快捷方式）
4. 下次可直接点击桌面图标打开

### Android（安卓手机）
1. 打开 Chrome 或其他浏览器
2. 在地址栏输入：`http://[电脑IP]:8000/pomodoro.html`
3. 点击右上角菜单 → "安装应用" 或 "添加到主屏幕"
4. 可作为应用使用

### 使用二维码
最简单的方式！
1. 访问 `http://[电脑IP]:8000/info`
2. 用手机相机或二维码应用扫描页面上的二维码
3. 自动打开番茄钟应用

---

## 📊 API 端点

启动服务器后，可以访问以下 API：

| 端点 | 说明 | 用途 |
|------|------|------|
| `/pomodoro.html` | 主应用 | Web 应用界面 |
| `/info` | 信息页面 | 显示服务器信息和二维码 |
| `/pomodoro-subscribe.ics` | ICS 日历 | 导入到日历应用 |
| `/api/pomodoro-data` | JSON 数据 | API 接口 |
| `/api/qr-code` | 二维码图片 | QR 码图像 |
| `/health` | 健康检查 | 服务器状态 |

---

## 🆘 常见问题

### Q: 手机连不上怎么办？

**A: 按以下顺序检查：**

1. ✅ 电脑和手机在同一 WiFi 网络
2. ✅ 服务器已启动（看到 `🍅 番茄钟 Web 服务器已启动`）
3. ✅ 防火墙允许 Python 访问网络
4. ✅ IP 地址正确（运行脚本时会显示）

### Q: 显示 "连接被拒绝"？

**A: 可能原因：**
- 服务器未启动
- 端口号错误
- 防火墙阻止（Windows 用户需要允许）

**解决方案：**
```
1. 重启服务器
2. 确保没有其他程序占用该端口
3. 检查防火墙设置
```

### Q: 如何在内网共享给团队？

**A: 步骤：**
1. 在有公网 IP 的服务器上运行脚本
2. 将 URL 分享给团队成员
3. 所有人可以访问同一个番茄钟应用

### Q: 数据会丢失吗？

**A: 不会。** 所有数据保存在浏览器本地存储（LocalStorage）中：
- 每个浏览器独立保存
- 刷新页面不会丢失
- 清除浏览器数据才会丢失
- 建议定期导出备份（CSV/Markdown）

### Q: 可以远程访问吗？

**A: 可以，但需要：**
1. 在路由器上设置端口转发
2. 或使用内网穿透工具（如 ngrok）
3. 或部署到云服务器（AWS、阿里云等）

### Q: 如何部署到公网？

**A: 推荐方案：**

方案 1：ngrok 内网穿透
```bash
# 安装 ngrok: https://ngrok.com
ngrok http 8000

# 会生成公网 URL，分享给团队
```

方案 2：部署到云服务器
```bash
# 上传文件到云服务器
scp -r . user@server:/app/pomodoro

# 在服务器上运行
python3 run_server.py 8000 0.0.0.0
```

---

## 🔒 安全建议

### 本地网络（推荐）
- 仅在内网使用，无需担心安全
- 数据保存在本地，不上传任何内容

### 公网使用
如要在公网上使用，建议：

1. **使用 HTTPS**
   ```bash
   # 生成自签名证书
   openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365
   ```

2. **添加身份验证**
   修改 `run_server.py` 添加密码保护

3. **使用 VPN 或代理**
   限制访问来源

4. **定期更新**
   检查安全补丁和依赖更新

---

## 📞 支持和反馈

- GitHub Issues: [番茄钟仓库]
- 邮件: support@pomodoro.local
- 文档: https://pomodoro.local/docs

---

## 🎉 享受高效工作！

现在您可以在任何设备上使用番茄钟了：
- 📱 手机
- 📱 平板
- 💻 电脑
- ⌚ 其他网络设备

祝您工作高效！🚀
